export class Oferta {

 constructor (public nombre: string, public descripcion: string,

 public imagenUrl: string, public empresa: string, public fechaPubli: string, public ciudad: string, public tipoDeOferta: string, public web: string) {


// hay que ampliar este constructor para que en la drescripcion aparezcan mas detalles
    }

}
